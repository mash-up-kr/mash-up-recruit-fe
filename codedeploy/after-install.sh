#!/bin/bash

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

REPOSITORY=/home/ubuntu/deploy/mash-up-dev-recruit-fe

cd $REPOSITORY
yarn install
pm2 restart recruit