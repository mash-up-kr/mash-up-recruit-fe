import { fonts } from './fonts';
import { colors } from './colors';
import { zIndex } from './zIndex';
import { input } from './input';

export const theme = {
  fonts,
  colors,
  zIndex,
  input,
};
